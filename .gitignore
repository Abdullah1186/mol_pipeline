# vs code
.vscode/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# macOS
.DS_Store

# Pipeline outputs (CLI runs now land under artifacts/cli/; the rest is
# belt-and-braces in case output_dir is ever pointed at the repo root)
/artifacts/
/molecular_metrics.csv
/*_filtered*.db
/*.smiles.json
/*.ecomp.json

# Baseline reference data: keep the small CSVs tracked, ignore the heavy .db files
baselines/*.db

# Streamlit
.streamlit/

# User-uploaded DBs via the Streamlit UI (one subdir per session)
/.uploads/
